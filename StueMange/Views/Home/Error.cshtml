@model ErrorViewModel
@{
    ViewData["Title"] = "خطأ";
}

<div class="container text-center py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <i class="fas fa-exclamation-triangle fa-5x text-danger mb-4 animate__animated animate__bounce"></i>
            <h1 class="display-4 fw-bold">حدث خطأ غير متوقع</h1>
            <p class="lead text-muted">
                نأسف، لقد واجه النظام مشكلة أثناء معالجة طلبك.
                نحن نعمل على إصلاح المشكلة في أقرب وقت ممكن.
            </p>

            <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-4">
                <i class="fas fa-home me-2"></i> العودة إلى الصفحة الرئيسية
            </a>

            @if (Model.ShowRequestId)
            {
                <div class="alert alert-secondary mt-4" style="font-size: 0.9rem;">
                    <p>
                        <strong>معرف الطلب:</strong> <code>@Model.RequestId</code>
                    </p>
                    <small>يمكن أن يساعد هذا المعرف فريق الدعم الفني في تشخيص المشكلة.</small>
                </div>
            }
        </div>
    </div>
</div>
