@model RegisterTeacherViewModel

@{
    ViewData["Title"] = "تسجيل مدرس جديد";
    Layout = "_Layout";
}

<div class="account-wrapper">
    <div class="form-panel">
        <div class="form-container">
            <h1>إنشاء حساب مدرس</h1>
            <p>أدخل بياناتك لإكمال عملية التسجيل.</p>
            <form asp-action="RegisterTeacher" method="post" class="account-form">
                <div asp-validation-summary="All" class="text-danger mb-3"></div>
                <div class="row">
                    <div class="col-md-6 mb-4 text-end">
                        <label asp-for="FullName" class="form-label"></label>
                        <div class="input-group">
                            <input asp-for="FullName" class="form-control" />
                            <i class="bi bi-person input-group-icon"></i>
                        </div>
                    </div>
                    <div class="col-md-6 mb-4 text-end">
                        <label asp-for="Subject" class="form-label"></label>
                        <div class="input-group">
                            <input asp-for="Subject" class="form-control" />
                            <i class="bi bi-book input-group-icon"></i>
                        </div>
                    </div>
                </div>
                <div class="mb-4 text-end">
                    <label asp-for="Username" class="form-label"></label>
                    <div class="input-group">
                        <input asp-for="Username" class="form-control" />
                        <i class="bi bi-at input-group-icon"></i>
                    </div>
                </div>
                <div class="mb-4 text-end">
                    <label asp-for="Password" class="form-label"></label>
                    <div class="input-group">
                        <input asp-for="Password" class="form-control" type="password" />
                        <i class="bi bi-lock input-group-icon"></i>
                    </div>
                </div>
                <div class="mb-4 text-end">
                    <label asp-for="ConfirmPassword" class="form-label"></label>
                    <div class="input-group">
                        <input asp-for="ConfirmPassword" class="form-control" type="password" />
                        <i class="bi bi-lock-fill input-group-icon"></i>
                    </div>
                </div>
                <div class="d-grid mt-4 " style="margin:20px 0 !important">
                    <button type="submit" class="btn btn-primary btn-submit text-white" style="font-family:Cairo;">إنشاء الحساب</button>
                </div>
                <p class="bottom-link">لديك حساب بالفعل؟ <a asp-action="Login">تسجيل الدخول</a></p>
            </form>
        </div>
    </div>
    <div class="branding-panel">
        <div class="branding-content">
            <h2>StueMange</h2>
            <p>نظام متكامل لمتابعة أبنائكم وتسهيل التواصل بين أولياء الأمور، المدرسين، والإدارة.</p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

